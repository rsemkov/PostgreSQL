SELECT CONCAT_WS(' ', name, state) AS "cities_information", area as "area_km2"
FROM cities;

-- From the selected already data combine the "name" and "state", 
-- fields into a single field called "cities_information". Rename the "area" column as "area_km2".
-- Submit your query for this task in the Judge system.
