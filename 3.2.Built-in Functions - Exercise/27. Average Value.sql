SELECT
	AVG(multiplication) as avarage_value
FROM 
	bookings_calculation


-- Create an SQL query that utilizes the AVG() function to calculate the average value of the 
-- "multiplication" column in the "bookings_calculation" table.
